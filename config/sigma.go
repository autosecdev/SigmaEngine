package config

// SigmaConfig is a struct that contains information about the Sigma configuration
type SigmaConfig struct {
	// RuleDir is the directory where the Sigma rules are stored
	RuleDir string
}
